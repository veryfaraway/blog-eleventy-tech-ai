---
permalink: /env-check/
eleventyExcludeFromCollections: true
---
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Environment Variables Check</title>
    <style>
        body {
            font-family: monospace;
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background: #f5f5f5;
        }
        .check-item {
            background: white;
            padding: 15px;
            margin: 10px 0;
            border-radius: 5px;
            border-left: 4px solid #ccc;
        }
        .check-item.success {
            border-left-color: #10b981;
        }
        .check-item.error {
            border-left-color: #ef4444;
        }
        .label {
            font-weight: bold;
            color: #333;
        }
        .value {
            color: #666;
            margin-top: 5px;
        }
        .status {
            display: inline-block;
            padding: 2px 8px;
            border-radius: 3px;
            font-size: 12px;
            margin-left: 10px;
        }
        .status.ok {
            background: #10b981;
            color: white;
        }
        .status.fail {
            background: #ef4444;
            color: white;
        }
    </style>
</head>
<body>
    <h1>🔍 Environment Variables Check</h1>
    <p>이 페이지는 환경 변수가 제대로 로드되었는지 확인합니다.</p>
    <p><strong>⚠️ 확인 후 이 페이지를 삭제하거나 접근을 차단하세요!</strong></p>
    
    <hr>
    
    <div class="check-item {% if env.googleAnalytics %}success{% else %}error{% endif %}">
        <div class="label">
            Google Analytics
            <span class="status {% if env.googleAnalytics %}ok{% else %}fail{% endif %}">
                {% if env.googleAnalytics %}✓ SET{% else %}✗ NOT SET{% endif %}
            </span>
        </div>
        <div class="value">
            {% if env.googleAnalytics %}
                Value: {{ env.googleAnalytics }}
            {% else %}
                환경 변수 GOOGLE_ANALYTICS_ID가 설정되지 않았습니다.
            {% endif %}
        </div>
    </div>
    
    <div class="check-item {% if env.adsense.enabled %}success{% else %}error{% endif %}">
        <div class="label">
            AdSense Enabled
            <span class="status {% if env.adsense.enabled %}ok{% else %}fail{% endif %}">
                {% if env.adsense.enabled %}✓ ENABLED{% else %}✗ DISABLED{% endif %}
            </span>
        </div>
        <div class="value">
            Value: {{ env.adsense.enabled }}
            {% if not env.adsense.enabled %}
                <br>환경 변수 ADSENSE_ENABLED를 "true"로 설정하세요.
            {% endif %}
        </div>
    </div>
    
    <div class="check-item {% if env.adsense.client %}success{% else %}error{% endif %}">
        <div class="label">
            AdSense Client ID
            <span class="status {% if env.adsense.client %}ok{% else %}fail{% endif %}">
                {% if env.adsense.client %}✓ SET{% else %}✗ NOT SET{% endif %}
            </span>
        </div>
        <div class="value">
            {% if env.adsense.client %}
                Value: {{ env.adsense.client }}
            {% else %}
                환경 변수 ADSENSE_CLIENT_ID가 설정되지 않았습니다.
            {% endif %}
        </div>
    </div>
    
    <div class="check-item {% if env.adsense.slots.inArticle %}success{% else %}error{% endif %}">
        <div class="label">
            AdSense In-Article Slot
            <span class="status {% if env.adsense.slots.inArticle %}ok{% else %}fail{% endif %}">
                {% if env.adsense.slots.inArticle %}✓ SET{% else %}✗ NOT SET{% endif %}
            </span>
        </div>
        <div class="value">
            {% if env.adsense.slots.inArticle %}
                Value: {{ env.adsense.slots.inArticle }}
            {% else %}
                환경 변수 ADSENSE_SLOT_IN_ARTICLE이 설정되지 않았습니다.
            {% endif %}
        </div>
    </div>
    
    <div class="check-item {% if env.adsense.slots.display %}success{% else %}error{% endif %}">
        <div class="label">
            AdSense Display Slot
            <span class="status {% if env.adsense.slots.display %}ok{% else %}fail{% endif %}">
                {% if env.adsense.slots.display %}✓ SET{% else %}✗ NOT SET{% endif %}
            </span>
        </div>
        <div class="value">
            {% if env.adsense.slots.display %}
                Value: {{ env.adsense.slots.display }}
            {% else %}
                환경 변수 ADSENSE_SLOT_DISPLAY가 설정되지 않았습니다.
            {% endif %}
        </div>
    </div>
    
    <hr>
    
    <h2>📋 체크리스트</h2>
    <ul>
        <li>{% if env.googleAnalytics %}✅{% else %}❌{% endif %} Google Analytics ID</li>
        <li>{% if env.adsense.enabled %}✅{% else %}❌{% endif %} AdSense Enabled</li>
        <li>{% if env.adsense.client %}✅{% else %}❌{% endif %} AdSense Client ID</li>
        <li>{% if env.adsense.slots.inArticle %}✅{% else %}❌{% endif %} AdSense In-Article Slot</li>
        <li>{% if env.adsense.slots.display %}✅{% else %}❌{% endif %} AdSense Display Slot</li>
    </ul>
    
    <hr>
    
    <h2>🔧 Netlify 환경 변수 설정 방법</h2>
    <ol>
        <li>Netlify 대시보드 접속</li>
        <li>Site settings > Environment variables</li>
        <li>다음 변수들을 추가:
            <ul>
                <li><code>GOOGLE_ANALYTICS_ID</code> = G-XXXXXXXXXX</li>
                <li><code>ADSENSE_ENABLED</code> = true</li>
                <li><code>ADSENSE_CLIENT_ID</code> = ca-pub-XXXXXXXXXXXXXXXX</li>
                <li><code>ADSENSE_SLOT_IN_ARTICLE</code> = 1234567890</li>
                <li><code>ADSENSE_SLOT_DISPLAY</code> = 0987654321</li>
            </ul>
        </li>
        <li>사이트 재배포</li>
    </ol>
    
    <p><a href="/">← 홈으로 돌아가기</a></p>
</body>
</html>
